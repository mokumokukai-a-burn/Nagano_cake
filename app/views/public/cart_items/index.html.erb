<h3>ショッピングカート</h3>
<%= link_to "カートを空にする", method: :delete %>
  <table>
	  <tr>
        <th>商品名</th>
        <th>単価（税込）</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
      </tr>

<% @cart_items.each do |cart_item| %>
<td></td>
<td></td>
<td></td>
<td></td>
<td><%= link_to "削除する", public_cart_items_path, method: :delete %></td>
<% end %>
</tr>

</tbody>
  </table>

<div>
<%= link_to "買い物を続ける", public_products_path, class:"btn btn-primary" %></div>

</div>

<div>
<%= link_to "情報入力に進む", class: "btn btn-success btn-lg" %>

</div>
